logfile: /tmp/bipbip.log
loglevel: DEBUG
logfile_days_to_keep: 5
logfile_maxsize: 1024*1024
frequency: 15
include: sample_base_includes.d/

storages:
  -
    name: copperegg
    api_key: MOCK_APIKEY

services:
  - 
    plugin: mysql
    username: travis
    password: 
    hostname: localhost
    port: 3306
